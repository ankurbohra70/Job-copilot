spring:
  application:
    name: job-copilot
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/job_copilot}
    username: ${DB_USERNAME:job_copilot}
    password: ${DB_PASSWORD:job_copilot}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    open-in-view: false
